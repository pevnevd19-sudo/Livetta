const fs=require('fs');const path=require('path');const Database=require('better-sqlite3');const root=path.resolve(__dirname,'..');const dir=path.join(root,'backups');if(!fs.existsSync(dir))fs.mkdirSync(dir,{recursive:true});const stamp=new Date().toISOString().replace(/[:.]/g,'-');const db=new Database(path.join(root,'data','database.sqlite'));db.backup(path.join(dir,`database-${stamp}.sqlite`)).then(()=>{console.log('Backup created');db.close();}).catch(e=>{console.error(e);process.exit(1);});
